pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                script {
                    echo '✅ Jenkinsfile 已加载！'
                    sh 'pwd && ls -la'
                }
            }
        }
    }
}
